# Media

## Collection overview

The Media collection stores uploaded files and their metadata.

## Available fields

- `alt` — alternative text
- `filename` — uploaded file name
- `mimeType` — detected MIME type
